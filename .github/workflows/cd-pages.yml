name: Deploy — GitHub Pages

on:
  workflow_call:
    inputs:
      working-directory:
        description: Path to the app (e.g. apps/client)
        type: string
        required: false
        default: '.'
      node-version:
        description: Node.js version
        type: string
        required: false
        default: '20'
      build-command:
        description: Build command to run (leave empty to auto-detect from package manager)
        type: string
        required: false
        default: ''
      output-directory:
        description: Build output directory relative to working-directory
        type: string
        required: false
        default: dist
    outputs:
      page-url:
        description: URL of the deployed GitHub Pages site
        value: ${{ jobs.deploy.outputs.page-url }}
    secrets:
      github-token:
        description: GitHub token passed as GITHUB_TOKEN env var to the build step (e.g. for scripts that call the GitHub API)
        required: false

jobs:
  build:
    name: Build (${{ inputs.working-directory }})
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        id: setup
        uses: KevinDeBenedetti/github-workflows/.github/actions/setup-node@main
        with:
          node-version: ${{ inputs.node-version }}
          working-directory: ${{ inputs.working-directory }}

      - name: Build
        working-directory: ${{ inputs.working-directory }}
        env:
          GITHUB_TOKEN: ${{ secrets.github-token }}
        run: |
          CMD="${{ inputs.build-command }}"
          if [ -z "$CMD" ]; then
            CMD="${{ steps.setup.outputs.package-manager }} run build"
          fi
          eval "$CMD"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ${{ inputs.working-directory }}/${{ inputs.output-directory }}

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.pages.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    outputs:
      page-url: ${{ steps.pages.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: pages
        uses: actions/deploy-pages@v4

      - name: Summary
        run: |
          {
            echo "### 📄 GitHub Pages Deployed"
            echo ""
            echo "🌍 **URL:** ${{ steps.pages.outputs.page_url }}"
          } >> "$GITHUB_STEP_SUMMARY"
